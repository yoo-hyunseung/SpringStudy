<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:p="http://www.springframework.org/schema/p"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">
	<!-- 사용자 정의 클래스 메모리 할당 요청 -->
	<context:component-scan base-package="com.sist.*"/>
	<!-- 
	클래스 위에 어노테이션을 쓰면 메모리 할당 메모리 별로 메모리할당 구분
	@Component : 일반 클래스 (MainClass, ~Manager)
	@Repository, : DAO(데이터 저장소)
	@Service, : BI(DAO 여러개를 하나로 합쳐서 사용할 때) 게시판+댓글
	@Controller, : Model(web)
	@RestController, : Model(Rest -> JSON 전송..) Front 
	@ControllerAdvice, : 공통기반의 예외처리
	@Configuration : 자바로 스프링 설정할 때 -> app.xml대체
	
	DI : 주입(객체주소)
	@Autowired, : 자동 주입 (스프링에서 해당 클래스를 찾아서 자동으로 주입) 객체 주소를 얻어온다.'
	@PostConstruct, : init-method
	@PreDestroy, : destroy-method
	@Resource, : 직접 객체 주소를 요청 (JDK 1.8 까지만 사용이 가능)
	
	 -->
<!-- database 관련 -->
	<bean id="ds" class="org.apache.commons.dbcp.BasicDataSource"
		p:driverClassName="oracle.jdbc.driver.OracleDriver"
		p:url="jdbc:oracle:thin:@211.238.142.118:1521:xe"
		p:username="hr"
		p:password="happy"
	/>
	
	<!-- mybatis 연결 -->
	<bean id="ssf" class="org.mybatis.spring.SqlSessionFactoryBean"
		p:dataSource-ref="ds"
	/>
	<!-- mybatis에서 interface 구현 -->
	<bean id="mapper" class="org.mybatis.spring.mapper.MapperFactoryBean"
		p:sqlSessionFactory-ref="ssf"
		p:mapperInterface="com.sist.mapper.EmpMapper"
	/>
</beans>
